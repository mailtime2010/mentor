package calculator.operators;

/**
 * Actions.
 *
 * @author Maxim Vanny
 * @version 5.0
 * @since 10/15/2019
 */
public interface Actions {
    /**
     * Method to execute the two numbers.
     *
     * @param one one number
     * @param two two number
     * @return a result of execute
     */
    int execute(int one, int two);

}
